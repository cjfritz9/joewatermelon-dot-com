import { Metadata } from "next";

export const metadata: Metadata = {
  title: "ToB Speed Guide",
  description: "Learn how to prepare for Theatre of Blood 4 & 5-man Grandmaster speed runs. Gear requirements, strategy tips, and what to expect.",
};

export default function TobInfoLayout({ children }: { children: React.ReactNode }) {
  return children;
}
